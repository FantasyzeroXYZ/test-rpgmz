# 游戏模拟器迁移修复文档 — 方法论与调试逻辑

## 一、架构理解：两个项目之间的根本差异

原项目（test-rpgmz-main）和新项目（React + Vite）虽然运行相同的 WASM 虚拟文件系统引擎，但**资源加载链路完全不同**。

原项目是单 HTML 文件，所有 JavaScript 内联在 `<script>` 标签中。沙箱拦截器通过 JavaScript 模板字面量直接生成，在浏览器运行时一次性完成转义处理和代码注入。

新项目经历了三层加工：TypeScript 编译 → Vite 打包 → React 组件渲染 iframe。每一层都可能改变代码的语义，尤其是字符串转义和模块加载路径。

理解这个差异是后续所有调试的出发点。

---

## 二、调试工具链

### 2.1 文件级静态分析（Node.js 脚本）

在无法直接观察浏览器行为时，通过分析游戏 ZIP 的文件结构来验证资源可达性。核心思路：模拟 VFS 的路径解析逻辑，逐一验证游戏 HTML 中引用的每个脚本、样式、字体是否确实存在于 ZIP 包内。

工具链：
- `jszip` — 读取 ZIP 文件结构
- 正则提取 — 从 HTML/CSS/JS 中提取资源引用路径
- 路径模拟 — 实现 `normalizePath` 和 `_resolvePath` 的等价逻辑

### 2.2 浏览器自动化测试（Puppeteer + Edge）

静态分析只能验证"文件存在"，无法验证"运行时可加载"。浏览器自动化测试通过以下方式捕获运行时错误：

- `page.on('console')` — 捕获 `console.error`、未捕获异常
- `page.on('pageerror')` — 捕获全局 `window.onerror`
- `page.on('response')` — 监控所有 HTTP 响应状态码，精确定位 404 资源
- `page.evaluate()` — 在页面上下文中执行查询（iframe 是否存在、VFS 是否可访问）

这个工具链的价值在于：它能发现 ZIP 文件分析完全无法发现的错误——比如 Script.src 拦截器是否**实际生效**，而非仅检查拦截器代码是否存在于沙箱中。

### 2.3 编译产物验证

直接检查 Vite/Rollup 构建后的 JS bundle，验证关键代码是否被正确嵌入。具体做法是 `grep` 查找 bundle 中的关键标识符（`getVfs`、`HTMLScriptElement`、`normalizePath`），确认：
1. 沙箱代码是否被包含在最终的 JS 输出中
2. 正则表达式是否被正确处理
3. 是否有意外的代码删除或转换

---

## 三、关键错误的分析逻辑

### 3.1 WASM 403（文件服务边界）

**调试线索**：错误信息中包含 `/@fs/` 前缀路径。这是 Vite 特有的文件系统服务路径格式，表明 Vite 正在尝试从文件系统提供文件，而非从 `public/` 静态目录。

**推理过程**：如果文件在 `public/` 内，Vite 会直接以 HTTP 路径提供服务（不会出现 `/@fs/`）。出现 `/@fs/` 意味着 Vite 通过文件系统解析到了项目目录外的路径——触发了安全限制。

**解决思路**：将文件放入 Vite 认可的服务范围内（`public/`），同时配置 `server.fs.allow` 作为开发阶段的补充保障。

### 3.2 iframe 跨域（浏览器安全模型）

**调试线索**：错误信息 `origin "null"` 是关键。在浏览器安全模型中，`srcdoc` iframe 默认 origin 为 `null`，与任何真实 origin 都不同。

**推理过程**：沙箱代码需要访问 `window.parent.__vfsRef`。如果 iframe 和父页面 origin 不同，浏览器会阻止跨域访问。`null` origin 与父页面的 `http://localhost:3013` 必然不同，访问一定被阻止。

**解决思路**：`allow-same-origin` 属性使 `srcdoc` iframe 继承父页面的 origin，绕过同源策略。Chrome 关于"escape its sandboxing"的警告信息是指理论上 iframe 内的代码可以访问父页面的 DOM——但因为我们完全控制 iframe 内容，这不是实际风险。

### 3.3 脚本 404（浏览器资源加载管道的理解）

**调试线索**：ZIP 文件分析显示所有脚本文件确实存在，但浏览器仍然报 404。这说明文件存在性没问题，问题出在"浏览器如何获取这些文件"。

**推理过程**：浏览器有多种资源加载机制：
- `fetch()` 和 `XMLHttpRequest` —— 可以被 JavaScript 拦截
- `<script src="...">` 的 HTML 解析器加载 —— **绕过所有 JavaScript 拦截**
- `new Worker(url)` —— **完全独立的加载管道**
- CSS `url()` 字体加载 —— **浏览器 CSS 引擎直接请求**

理解了这些加载机制的区别后，就能明白为什么只拦截 `fetch` 和 `XHR` 是不够的——游戏引擎使用 `script.src = "..."` 加载脚本，这走的是第二条路径。

**解决思路**：对每种加载机制使用对应的拦截方法：
- HTML 中的 `<script src>` → 构建时正则替换
- 运行时 `script.src = "..."` → 原型 setter 拦截
- Worker → 构造函数拦截
- 字体 CSS `url()` → FontFace 构造函数拦截 + CSS 文本重写

### 3.4 正则语法错误（字符转义链的追踪）

**调试线索**：构建成功（TypeScript 编译通过），但浏览器运行时报正则语法错误。这说明问题出在"编译产物"与"浏览器解释"之间的差异。

**推理过程**：需要追踪字符从源文件到浏览器解析器的完整路径：

```
test-rpgmz-main/index.html (模板字面量源码)
    → 提取到 public/sandbox.js (文件系统)
    → Vite ?raw 导入 (作为字符串)
    → TypeScript 模板字面量 ${sandboxCode}
    → 构建产物 (dist/assets/index-xxx.js 中的字符串)
    → HTML <script> 标签
    → 浏览器 JavaScript 解析器
```

每一步都可能有转义规则的差异。关键发现：
- 模板字面量中的 `\\/` 在运行时会被处理为 `/`
- `?raw` 导入不经过模板字面量处理——它返回的是文件的原始字节
- 当这些原始字节被直接嵌入输出 HTML 时，浏览器看到的仍是模板源码中的 `\\/`，而非处理后的 `\/`

定位方法：使用 `node --check` 直接检查 `public/sandbox.js` 的语法时会报错（因为文件包含模板转义），但构建成功（因为 TypeScript 编译器在处理模板字面量时会进行转义）。这个矛盾本身就是线索——证实了问题出在转义层次。

**解决思路**：编写字符级转换器，模拟模板字面量的转义规则，将模板源码中的转义字符转换为原始 JavaScript 中对应的字面字符。

### 3.5 二次解密（引擎内部流程的理解）

**调试线索**：游戏在 iframe 中创建成功（沙箱运行正常），但画面停留在加载状态。没有明确的错误信息。

**推理过程**：游戏引擎的初始化流程是：加载 System.json → 加载图片资源 → 加载音频资源 → 初始化场景 → 显示画面。如果卡在加载状态，说明资源加载环节出了问题。

VFS 在 `read_file_decrypted` 中已经完成 XOR 解密。引擎的 `Utils.decryptArrayBuffer` 在 XHR 加载完成后会再次解密。两次 XOR 操作会互相抵消（第一次正确解密，第二次用同样的密钥再次 XOR = 回到加密状态）。

**解决思路**：需要理解引擎的解密触发条件。引擎通过 `Utils.hasEncryptedImages()` 和 `Utils.hasEncryptedAudio()` 判断是否需要解密。将这两个函数返回值改为 `false`，引擎就会使用 XHR/fetch 的原始响应数据——而这些数据已经被 VFS 解密过了。同时将 `Utils.decryptArrayBuffer` 改为无操作函数，作为双重保险。

### 3.6 normalizePath 前导斜杠（路径规范化的边界情况）

**调试线索**：大多数游戏的脚本可以加载，但特定游戏的某些脚本仍然 404。

**推理过程**：VFS 内部使用两种路径：ZIP 文件中的原始路径（如 `js/main.js`，无前导 `/`）和带前导 `/` 的绝对路径（从 HTTP URL 提取）。`fileExists` 使用精确匹配和模糊匹配。对于简单的目录结构（文件就在 ZIP 根目录下），两种路径都能匹配。但对于有 `www/` 前缀的游戏，路径解析可能失败。

**解决思路**：在 `normalizePath` 中统一去除前导 `/`，使 VFS 的所有路径查询都使用无前缀格式。

---

## 四、测试驱动开发的循环

整个修复过程遵循了以下循环：

1. **编写测试**：`tests/full-pipeline-test.mjs` 对 9 个游戏 ZIP 进行分析
2. **运行测试**：观察哪些游戏通过、哪些失败
3. **分析失败**：定位具体缺失的资源或错误的逻辑
4. **修复**：修改代码
5. **构建 + 浏览器测试**：`tests/auto-browser-test.mjs` 使用 Puppeteer 验证运行时行为
6. **重复**：直到所有游戏通过

这个循环的关键价值在于：静态分析只能验证"应该能工作"，浏览器测试才能验证"确实在工作"。很多错误（如正则转义、二次解密）在静态分析中完全不可见，只有在浏览器实际执行时才会暴露。

---

## 五、关键收获

1. **字符转义是隐形的杀手**：当数据流经多个处理层（模板字面量 → 文件提取 → 模块导入 → 模板嵌入 → HTML 注入 → 浏览器解析），每一层的转义规则都必须被正确理解和处理。解决这类问题的方法是追踪完整的字符流路径。

2. **浏览器的资源加载不是统一的**：脚本、样式、图片、Worker、字体各有独立的加载管道。不能假设拦截了 `fetch` 和 `XHR` 就拦截了所有网络请求。

3. **理解引擎的内部逻辑比照搬代码更重要**：知道为什么 `Utils.decryptArrayBuffer` 会导致二次解密，比机械地复制覆盖代码更有价值。这决定了补丁是"恰好能工作"还是"从根本上正确"。

4. **自动化测试的价值在于捕获"未知的未知"**：手动测试会遗漏间歇性问题（如特定游戏、特定脚本的 404），自动化测试能系统性地覆盖所有场景。
